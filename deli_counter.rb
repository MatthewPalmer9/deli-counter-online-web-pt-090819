def line(array)
  if array.length < 1 
    puts "The line is currently empty."
  elsif array.length >= 1
    count = 0
    puts "The line is currently: " + array.each(){|person| 
      array[count+1].index
    }
end